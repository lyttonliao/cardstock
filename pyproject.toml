[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cardstock"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["api*", "core*", "ingestion*", "ml*", "pipeline*"]
